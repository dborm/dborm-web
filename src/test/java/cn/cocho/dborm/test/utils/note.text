编码规范：Dborm开头的类，属于该框架的专属类，如DbormContextsDborm结尾的类，CodeReview（一）  2014-07-16    蒋凌峰、宋海康提出的建议如下：1、数据库链接现在使用的是Connection类，可以考虑使用DataSource2、类命名：SQLTranslater、SQLExcuter、ResultMapper、DataTypeConverter3、静态函数只要所有的变量都属于局部变量则属于线程安全的4、ColumnName属性还是需要5、查询功能可以进一步增强，比如添加函数selectByExmple6、可以添加带自动映射接口的查询函数数据库链接的创建和获取以接口的方式公开给用户X2.批量删除使用一条SQL语句，用inX3.查询时支持in,传入集合Y1.注册Domain的时候支持包名MySql版本默认值只需要在属性上赋值即可数据库事务使用完毕要使用end释放去掉连接池添加注解支持初始化数据库的时候必须一个类一个类的添加，不能指定包名，因为当包名是某个jar包内的包名时就会出问题将属性的别名去掉注意事项：属性值必须使用大类型，比如，需要使用int的时候，应该使用Integer如果想要使用replace（修改属性值不为）操作，属性值一定不要有默认值，否则会导致，不给该属性赋值的时候replace认为该属性值不为null，仍然做update操作需要添加默认值，没有默认值时使用"null"字符串